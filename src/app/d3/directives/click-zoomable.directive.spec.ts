import { ClickZoomableDirective } from './click-zoomable.directive';

describe('ClickZoomableDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickZoomableDirective();
    expect(directive).toBeTruthy();
  });
});
