import { DraggableNodeDirective } from './draggable-node.directive';

describe('DraggableNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableNodeDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
